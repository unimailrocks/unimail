machine:
  environment:
    DEPLOY_HOSTNAME: galaxy.meteor.com

dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh

deployment:
  alpha:
    branch: "alpha"
